% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ct-defaults.R
\name{ct_defaults}
\alias{ct_defaults}
\alias{ct_set_defaults}
\alias{ct_unset_defaults}
\title{Set or restore ggconsulting aesthetic defaults}
\usage{
ct_set_defaults()

ct_unset_defaults()
}
\value{
Both functions return \code{invisible(NULL)}.
}
\description{
\code{ct_set_defaults()} overrides ggplot2 \emph{aesthetic} defaults via
\code{\link[ggplot2:update_defaults]{ggplot2::update_geom_defaults()}}. v0.1 sets a single override:
\code{geom_point} \code{size = 2.5}. \code{ct_unset_defaults()} restores whatever
values were active the first time \code{ct_set_defaults()} ran — \emph{not}
ggplot2's untouched baseline.

Originals are captured once into a package-private environment, so
repeated \code{ct_set_defaults()} calls are idempotent. Column width and
linewidth are handled elsewhere — see \code{\link[=ct_col]{ct_col()}} / \code{\link[=ct_line]{ct_line()}} for
explicit formal overrides, or apply \code{\link[=ct_theme]{ct_theme()}} / \code{\link[=theme_strategy]{theme_strategy()}}
for linewidth via \code{from_theme()}.
}
\examples{
ct_set_defaults()
ct_unset_defaults()
}
