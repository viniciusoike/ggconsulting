% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ct-finish.R
\name{ct_finish}
\alias{ct_finish}
\title{Apply data-aware finishing touches to a plot}
\usage{
ct_finish(
  values = FALSE,
  sort = NULL,
  label_fmt = NULL,
  highlight = NULL,
  end_labels = FALSE,
  end_points = FALSE,
  axis_y = NULL,
  expand = "auto",
  muted_color = "#A8A4A0"
)
}
\arguments{
\item{values}{\code{TRUE} adds value labels above bars / next to points.
\code{"auto"} adds them only when the first geom is a column or bar.}

\item{sort}{One of \code{"asc"}, \code{"desc"}, or \code{NULL}. When set, reorders
the factor levels of the x aesthetic by the y aesthetic.}

\item{label_fmt}{Either a formatter function (anything that maps a
numeric vector to a character vector), or one of the shortcut names
\code{"brl"}, \code{"number"}, \code{"pct"}, \code{"delta"} resolved to the matching
\verb{fmt_*()} helper.}

\item{highlight}{Value(s) of the x aesthetic to emphasise. Matching
bars use the active palette's main colour; non-matching bars use
\code{muted_color}. Inserted as a \verb{scale_*_manual()}.}

\item{end_labels}{For line plots: \code{TRUE} labels the last point of each
series with the group identifier. \code{"first_facet"} draws those labels
only in the first panel, which replaces a legend across small
multiples; on an unfaceted plot it behaves like \code{TRUE}. The x
aesthetic may be numeric, \code{Date}, or \code{POSIXct}.}

\item{end_points}{For line plots: when \code{TRUE}, draw a filled point at
each series' last observation, under the end label.}

\item{axis_y}{Moves the y axis. \code{"right"} or \code{"left"}; \code{NULL} (default)
leaves it where the scale puts it. Applied through
\code{\link[ggplot2:guide_axis]{ggplot2::guide_axis()}}, so it does not disturb a \verb{scale_y_*()} call
of your own.}

\item{expand}{\code{"auto"} picks geom-aware scale expansion
(room above column tops, right-side room for line end labels);
\code{FALSE} disables. Auto expansion defers to a positional scale you
supplied yourself, so a \code{scale_y_continuous(labels = )} keeps its
labels; set the expansion in that call when you need both.}

\item{muted_color}{Fill / colour used for non-highlighted categories.}
}
\value{
A \code{ct_finish} object, added to a plot via \code{+}. The
\code{ggplot_add()} method composes the requested layers and scales.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Companion to \code{\link[=ct_theme]{ct_theme()}} that runs \emph{after} the geom layer is built,
so it can inspect the data + active geom to inject value labels,
sorting, highlighting, end labels, and scale expansion. Compose with
\code{+}, after the geoms.
}
\examples{
library(ggplot2)
d <- data.frame(g = LETTERS[1:5], v = c(3, 8, 5, 12, 7))
p <- ggplot(d, aes(g, v)) +
  geom_col() +
  ct_finish(values = TRUE, sort = "desc", label_fmt = "brl", highlight = "D") +
  theme_strategy()
}
