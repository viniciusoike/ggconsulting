% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format-helpers.R
\name{ct_formatters}
\alias{ct_formatters}
\alias{fmt_number}
\alias{fmt_brl}
\alias{fmt_currency}
\alias{fmt_pct}
\alias{fmt_delta}
\alias{fmt_month}
\title{Locale-aware label formatters}
\usage{
fmt_number(decimals = 0, locale = NULL)

fmt_brl(decimals = 2, style = c("minus", "accounting"), locale = NULL)

fmt_currency(decimals = 2, style = c("minus", "accounting"), locale = NULL)

fmt_pct(decimals = 1, locale = NULL, accuracy = NULL)

fmt_delta(decimals = 1, suffix = "pp", locale = NULL)

fmt_month(format = c("abbr", "full"), locale = NULL)
}
\arguments{
\item{decimals}{Number of decimal places.}

\item{locale}{Optional locale name. Defaults to the active locale.}

\item{style}{\code{"minus"} (default) or \code{"accounting"} for currency.}

\item{accuracy}{Optional explicit accuracy passed to
\code{\link[scales:percent_format]{scales::percent()}}; overrides \code{decimals} if set.}

\item{suffix}{Suffix for \code{fmt_delta()}. Defaults to \code{"pp"}.}

\item{format}{\code{"abbr"} (default) or \code{"full"} for \code{fmt_month()}.}
}
\value{
A formatter function: \code{function(x) character}.
}
\description{
Factory functions: each returns a function compatible with
\verb{scales::label_*} (callable on a numeric vector). Marks (thousands,
decimals) and currency symbols come from the active ggconsulting
locale, set via \code{\link[=ct_locale]{ct_locale()}}.
\itemize{
\item \code{fmt_number()} — plain numbers with locale separators.
\item \code{fmt_brl()} — Brazilian Real. Always renders as \verb{R$} regardless of
active locale; the \code{locale} argument lets you override marks.
\item \code{fmt_currency()} — uses the active locale's currency symbol.
\item \code{fmt_pct()} — percentages, interpreting input as a fraction
(\code{0.5} → \code{"50\%"}).
\item \code{fmt_delta()} — percentage-point-style signed deltas
(\verb{+1,2pp} / \verb{-0,3pp} / \verb{0,0pp}).
\item \code{fmt_month()} — Date / POSIXct → localised month string.
}

Negatives default to a minus prefix; currency helpers also support
\code{style = "accounting"}, which wraps negatives in parentheses. Currency
rendering uses a non-breaking space (\verb{U+00A0}) between symbol and
number so the pair never wraps across lines.
}
\examples{
fmt_number(decimals = 1)(1234.5)
fmt_brl()(c(1000, -500))
fmt_pct(decimals = 0)(0.5)
fmt_delta()(c(1.2, -0.3, 0))
fmt_month()(as.Date("2026-08-15"))
}
