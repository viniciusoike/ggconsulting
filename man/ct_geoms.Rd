% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ct-wrappers.R
\name{ct_geoms}
\alias{ct_geoms}
\alias{ct_col}
\alias{ct_line}
\alias{ct_point}
\title{Drop-in replacements for geom_col(), geom_line(), and geom_point()}
\usage{
ct_col(..., width = 0.8)

ct_line(..., linewidth = 0.7)

ct_point(..., size = 2.5)
}
\arguments{
\item{...}{Forwarded to the underlying ggplot2 constructor.}

\item{width}{Column width. Defaults to 0.8 (vs ggplot2's 0.9).}

\item{linewidth}{Line width. Defaults to 0.7 (vs ggplot2's 0.5).}

\item{size}{Point size. Defaults to 2.5 (vs ggplot2's 1.5).}
}
\value{
A ggplot2 \code{\link[ggplot2:layer]{ggplot2::layer()}} object.
}
\description{
Three-line pass-throughs that ship consulting-grade defaults for
\code{\link[ggplot2:geom_bar]{ggplot2::geom_col()}} \code{width}, \code{\link[ggplot2:geom_path]{ggplot2::geom_line()}} \code{linewidth}, and
\code{\link[ggplot2:geom_point]{ggplot2::geom_point()}} \code{size}. Use them when you want the defaults
baked into the call site; plain \code{geom_line()} will also pick up
linewidth from \code{\link[=ct_theme]{ct_theme()}} / \code{\link[=theme_strategy]{theme_strategy()}} via \code{from_theme()},
and \code{geom_point()} size is autoloaded by \code{\link[=ct_set_defaults]{ct_set_defaults()}}.
}
\section{Bar charts with Date x}{

\code{ct_col()}'s default \code{width = 0.8} is in \emph{x-axis units}. On a Date
x-axis that is 0.8 \emph{days}, which renders bars as slivers when the
data is spaced quarterly or monthly. For bar charts, convert the
x variable to either an ordered factor (e.g. \code{"21Q1"}, \code{"21Q2"}, ...)
or a numeric index before plotting; line charts on Date are fine.
}

\examples{
library(ggplot2)
d <- data.frame(g = c("A", "B", "C"), v = c(3, 5, 2))
p_col   <- ggplot(d, aes(g, v)) + ct_col()
p_line  <- ggplot(economics, aes(date, unemploy)) + ct_line()
p_point <- ggplot(mtcars, aes(wt, mpg)) + ct_point()
}
