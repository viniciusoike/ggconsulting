% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palettes.R
\name{ct_palette}
\alias{ct_palette}
\title{Get palette colours}
\usage{
ct_palette(palette = NULL, n = NULL, reverse = FALSE)
}
\arguments{
\item{palette}{Palette name (e.g. \code{"strategy_navy"}) or \code{NULL} (default)
to list available palette names.}

\item{n}{Number of colours to return. When \code{n} is smaller than the
palette, the first \code{n} colours are returned. When \code{n} is larger,
colours are interpolated via \code{\link[grDevices:colorRamp]{grDevices::colorRampPalette()}} (with a
warning). Defaults to \code{NULL} (return the full palette).}

\item{reverse}{Reverse palette order before subsetting. Defaults to
\code{FALSE}.}
}
\value{
A character vector of hex colours, or (when \code{palette} is \code{NULL})
a character vector of palette names.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Returns the hex colour vector for a named palette, optionally subsetting
or interpolating to \code{n} colours. Called with no arguments, returns the
names of all available palettes.
}
\examples{
# List available palettes
ct_palette()

# Full palette
ct_palette("strategy_navy")

# First 3 colours
ct_palette("strategy_navy", n = 3)

# Interpolate to 9 colours
ct_palette("strategy_navy", n = 9)
}
