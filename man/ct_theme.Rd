% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ct-theme.R
\name{ct_theme}
\alias{ct_theme}
\title{Build a consulting-grade ggplot2 theme}
\usage{
ct_theme(
  palette = "strategy_navy",
  font = "Inter",
  density = c("normal", "tight", "loose"),
  context = c("presentation", "report", "screen"),
  base_size = NULL,
  main_color = NULL
)
}
\arguments{
\item{palette}{Palette name (e.g. \code{"strategy_navy"}) or character
vector of colours.}

\item{font}{Preferred font family. Falls back through
\code{c("Helvetica Neue", "Arial", "sans")} if unavailable.}

\item{density}{One of \code{"normal"}, \code{"tight"}, \code{"loose"}. Controls
\code{panel.spacing} and axis-text margins.}

\item{context}{One of \code{"presentation"}, \code{"report"}, \code{"screen"}. Drives
default \code{base_size} and \code{plot.margin}.}

\item{base_size}{Base font size. If \code{NULL}, derived from \code{context}.}

\item{main_color}{Colour used for the plot title and routed into the
theme \code{geom} element's \code{ink} slot, so unmapped geoms pick it up via
\code{from_theme()}. \code{NULL} falls back to the palette's first colour.}
}
\value{
A \code{\link[ggplot2:theme]{ggplot2::theme()}} object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Flexible builder behind the archetype presets (\code{\link[=theme_strategy]{theme_strategy()}} and
friends). Composes \code{\link[ggplot2:ggtheme]{ggplot2::theme_minimal()}} with executive-output
overrides via \verb{theme_sub_*()} helpers, sets the theme \code{geom} element
so \code{from_theme()}-aware layers (e.g. \code{\link[ggplot2:geom_path]{ggplot2::geom_line()}}) inherit
the resolved linewidth and main colour, and applies the resolved
font with a fallback chain.
}
\examples{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  ct_theme()
}
