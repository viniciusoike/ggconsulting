% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-font.R
\name{install_consulting_fonts}
\alias{install_consulting_fonts}
\title{Install consulting fonts from Google Fonts}
\usage{
install_consulting_fonts(fonts = NULL, dest = NULL, quiet = FALSE)
}
\arguments{
\item{fonts}{Character vector of family names to install, or \code{NULL}
(default) for the full set: Inter, Source Sans 3, Lato,
Source Serif 4, IBM Plex Sans. Validated against the known catalog.}

\item{dest}{Destination directory. \code{NULL} (default) uses a
platform-appropriate user font directory: \verb{~/Library/Fonts} on
macOS, \verb{~/.local/share/fonts} on Linux, or a session tempdir on
Windows (with \code{\link[systemfonts:register_font]{systemfonts::register_font()}} for the active session).}

\item{quiet}{Suppress informational messages. Errors are always
emitted. Defaults to \code{FALSE}.}
}
\value{
Invisibly, a character vector of installed file paths.
}
\description{
Downloads and installs the font families used by the three archetype
themes (\code{\link[=theme_strategy]{theme_strategy()}}, \code{\link[=theme_finance]{theme_finance()}}, \code{\link[=theme_editorial]{theme_editorial()}}).
All fonts are OFL or Apache-2.0 licensed.
}
\details{
Variable-weight families (Inter, Source Sans 3, Source Serif 4,
IBM Plex Sans) are downloaded as variable \code{.ttf} files containing all
weights (Light through Bold and beyond). Lato is downloaded as four
static \code{.ttf} files (Regular, Bold, Italic, Light).
}
\examples{
\dontrun{
install_consulting_fonts()
install_consulting_fonts("Inter")
}
}
