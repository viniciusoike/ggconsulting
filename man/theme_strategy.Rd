% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme-strategy.R
\name{theme_strategy}
\alias{theme_strategy}
\title{Strategy archetype theme}
\usage{
theme_strategy(main_color = NULL, ...)
}
\arguments{
\item{main_color}{Routed into the theme \code{geom} \code{ink} slot so unmapped
geoms pick it up via \code{from_theme()}. Defaults to \code{strategy_navy[1]}.
Pass any value \code{grDevices::col2rgb()} accepts to override. Does not
affect title colour (a fixed neutral near-black).}

\item{...}{Forwarded to \code{\link[=ct_theme]{ct_theme()}} — e.g. \code{density}, \code{context},
\code{base_size}.}
}
\value{
A \code{\link[ggplot2:theme]{ggplot2::theme()}} object.
}
\description{
Preset path through \code{\link[=ct_theme]{ct_theme()}} tuned for a minimal,
generous-whitespace, navy-default look inspired by top-tier global
strategy consultancies.
}
\examples{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  theme_strategy()
}
